id = "ship"
name = "Ship workflow"
description = "Practice — end-to-end orchestrator (commit → review → PR). Read alongside commits / code-review / pull-requests."
version = "1.0.0"
kind = "practice"
practice = "practices/ship.md"

layers = ["harness"]

[requires]
git     = true
tools   = []
