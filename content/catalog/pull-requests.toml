id = "pull-requests"
name = "Pull request workflow"
description = "Practice — What/Why/How PRs with a Test plan; conventional-commit titles; provider notes (gh / GitLab / ADO)."
version = "1.0.0"
kind = "practice"
practice = "practices/pull-requests.md"

layers = ["harness", "conventions"]

[requires]
git     = true
tools   = ["gh"]
