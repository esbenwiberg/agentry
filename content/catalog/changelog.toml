id = "changelog"
name = "Changelog fragments"
description = "Practice — fragment-based changelog (one file per change) vs generated; when each fits, drift modes."
version = "1.0.0"
kind = "practice"
practice = "practices/changelog.md"

layers = ["harness", "conventions"]

[requires]
git     = true
tools   = []
