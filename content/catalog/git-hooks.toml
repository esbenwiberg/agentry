id = "git-hooks"
name = "Git hooks strategies"
description = "Practice — choosing a hook strategy (raw .githooks / husky / lefthook / pre-commit), writing hooks teams keep."
version = "1.0.0"
kind = "practice"
practice = "practices/git-hooks.md"

layers = ["harness", "validation"]

[requires]
git     = true
tools   = []
