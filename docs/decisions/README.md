# Decisions drawer

Free-form, unnumbered notes on design decisions still being argued over.

When a decision locks, write a numbered ADR in `../adr/` and either delete
the corresponding note here or leave a short tombstone linking to the ADR.

This drawer is the messy whiteboard. `../adr/` is the file cabinet.
