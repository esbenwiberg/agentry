---
type: FEAT
scope: scaffold
---

Bootstrap the agentry repository with Phase 0 scaffolding: bones (LICENSE, package.json, tsconfig, .editorconfig, .gitignore), dogfood docs (CLAUDE.md, PRACTICES.md), changelog plumbing, git hooks (commit-msg + pre-commit + secret-checks + install script), ADR convention (template, 0000, 0001 product posture lock), content tree placeholders, and a CLI stub. No verbs implemented yet; see ADR-0001 for the locked design.
