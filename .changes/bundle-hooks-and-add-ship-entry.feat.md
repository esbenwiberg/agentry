---
type: FEAT
scope: catalog
---

Lift git hooks and changelog scripts into the catalog so `agentry add` produces a working install. Bumps changelog and commits to 0.2.0, adds new git-hooks and ship entries, and preserves the executable bit when copying provided files.
