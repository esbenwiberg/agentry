---
type: FEAT
scope: cli
---

Add 'coach spec-init' and 'coach spec <slug>' to scaffold the specs/ kernel — README + _template/ (purpose, design, acceptance, briefs) on init, then per-feature folders rendered with title and date stamps. Closes the last Phase 1 kernel-extraction gap.
